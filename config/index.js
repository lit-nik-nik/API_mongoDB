const app = require('./app');
const express = require('./express');
const routesSettings = require('./routesSettings');
const routesCity = require('./routesCity');
const routesTime = require('./routesTime');

module.exports = {
    app,
    express,
    routesSettings,
    routesCity,
    routesTime
}